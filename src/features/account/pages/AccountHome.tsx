import PagePlaceholder from '../../../components/PagePlaceholder'

export default function AccountHome() {
  return (
    <PagePlaceholder
      title="My Account"
      phase="Phase 2 (auth) & Phase 5 (registration) build this"
      note="Registration status, confirmation, meal preference, visibility settings, and session access will live here."
    />
  )
}
