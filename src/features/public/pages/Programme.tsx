import PagePlaceholder from '../../../components/PagePlaceholder'

export default function Programme() {
  return <PagePlaceholder title="Programme" phase="Coming in a later phase" note="Data-backed page (Phase 4) — renders from Session records." />
}
