import PagePlaceholder from '../../../components/PagePlaceholder'

export default function AdminExport() {
  return <PagePlaceholder title="Data Export" phase="Coming in a later phase" note="Phase 7 — dataset + Excel/CSV export." />
}
